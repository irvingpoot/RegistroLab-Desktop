---

/**
 * @file 404.astro
 * @description Página de error para rutas no encontradas.
 * * PROPÓSITO:
 * - Informar al usuario que la URL solicitada no existe.
 * - Proporcionar un botón de retorno seguro al "Dashboard" para evitar
 * que el usuario quede atrapado.
 * * DISEÑO:
 * - Utiliza efectos visuales (Glow, Gradientes) consistentes con el tema de la app.
 */

import Layout from "../layouts/Layout.astro";
---

<Layout title="404 - Página no encontrada">
    <main class="flex flex-col items-center justify-center min-h-screen px-4 text-center relative overflow-hidden">
        
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[300px] md:w-[500px] h-[300px] md:h-[500px] bg-blue-600/20 rounded-full blur-[80px] md:blur-[120px] -z-10 animate-pulse"></div>

        <div class="z-10 space-y-6">
            <div class="relative">
                <h1 class="text-8xl md:text-9xl font-extrabold tracking-tighter">
                    <span class="bg-gradient-to-b from-white via-blue-200 to-blue-600 bg-clip-text text-transparent drop-shadow-[0_0_35px_rgba(37,99,235,0.6)]">
                        404
                    </span>
                </h1>
                <span class="absolute -top-4 -right-4 text-4xl md:text-6xl animate-bounce">🌙</span>
            </div>

            <h2 class="text-2xl md:text-4xl font-bold text-white">
                Página no encontrada
            </h2>
            
            <p class="text-gray-400 text-base md:text-lg max-w-md mx-auto leading-relaxed">
                Parece que te has perdido en el <span class="text-blue-400 font-semibold">sueño profundo</span>. La página que buscas no existe.
            </p>

            <div class="pt-8">
                <a href="/dashboard" class="inline-flex items-center justify-center gap-3 bg-blue-600 text-white text-lg font-semibold px-8 py-3 rounded-xl hover:bg-blue-500 hover:scale-105 hover:shadow-[0_0_25px_rgba(37,99,235,0.6)] transition duration-300 group">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-5 h-5 transition-transform group-hover:-translate-x-1">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5 3 12m0 0 7.5-7.5M3 12h18" />
                    </svg>
                    Volver al inicio
                </a>
            </div>
        </div>

        <div class="absolute bottom-10 text-gray-600 text-sm font-mono">
            Error: PAGE_NOT_FOUND_EXCEPTION
        </div>

    </main>
</Layout>